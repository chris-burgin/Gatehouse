
machine:
  python:
    version: pypy-2.2.1


dependencies:
  pre:
    - pip install flask
    - pip install Flask-SQLAlchemy
    - sudo pip install pycrypto
    - pip install Crypto
    - sudo apt-get install sqlite3
